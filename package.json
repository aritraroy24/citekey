{
  "name": "citekey",
  "version": "1.0.1",
  "private": true,
  "type": "module",
  "description": "Chrome extension that generates BibTeX with Google Scholar-matching citation keys.",
  "license": "MIT",
  "scripts": {
    "test": "node --test \"tests/*.test.mjs\"",
    "package": "node scripts/package.mjs",
    "preview": "node scripts/preview.mjs"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "puppeteer-core": "^25.11.0"
  }
}
